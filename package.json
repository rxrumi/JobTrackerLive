{
  "name": "job-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "scan": "wrangler tail --format pretty",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  },
  "dependencies": {
    "@supabase/ssr": "^0.10.3",
    "@supabase/supabase-js": "^2.106.2"
  }
}
